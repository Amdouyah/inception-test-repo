apiVersion: v1
kind: Service
metadata:
  name: wil-playground
  labels:
    app: wil-playground
spec:
  type: ClusterIP
  selector:
    app: wil-playground
  ports:
    - name: http
      port: 80
      targetPort: 8888
